# Personal-Portfolio

Simple portfolio scaffold.

How to use:

- Replace `resume.pdf` with your real PDF resume.
- Replace images in the `images/` folder with actual photos.
- Edit `index.html`, `css/style.css`, and `js/script.js` to customize content and styles.

Open `index.html` in your browser to preview.